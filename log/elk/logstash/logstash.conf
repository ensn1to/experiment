input {
  # tcp server方式，应用上报
  # tcp {
  #   mode => "server"
  #   host => "0.0.0.0"
  #   port => 4560
  #   codec => json
  # }


  # redis 拉取方式
  redis {
		data_type => "list"
		key => "log_list"
		host => "redis"
		port => 6379
		db => 0
		threads => 2
	}
}
output {
  elasticsearch {
    hosts => "es:9200"
    index => "elk-%{+YYYY.MM.dd}"
  }
}
